[project]
name = "darien-gap"
version = "0.0.1"
description = "What can you find here?"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.14.1",
    "litestar[standart]>=2.14.0",
    "psycopg[binary,pool]>=3.2.4",
    "sqlalchemy>=2.0.37",
]
